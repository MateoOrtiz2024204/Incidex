PORT=3000
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=
DB_NAME=dbgestionincidencias_in5cm
# Ponlo en true si tu base de datos requiere SSL (por ejemplo Neon, Render, Supabase).
# En tu computadora local con PostgreSQL normal, dejalo en false.
DB_SSL=false
JWT_SECRET=incidex_secret_key
CORS_ORIGIN=http://localhost:4200
